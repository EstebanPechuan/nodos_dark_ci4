(function () {
  $.MultiLanguage = function (a, c) {
    let page = window.location.pathname
    var b = this;
    var pagina = page
    console.log(a, c, page);
    if (c == null) {
      c = null;
    }
    return $.getJSON(a, pagina, function (g) {
      var f, d, h, e;
      if (c !== null) {
        localStorage.MultiLanguage = c;
      } else {
        if (typeof localStorage.MultiLanguage === "undefined") {
          c = localStorage.MultiLanguage = g.config["home"];
        } else {
          c = localStorage.MultiLanguage;
        }
      }
      if (pagina === '/nodos_idiomas/' || pagina === '/nodos_idiomas/home') {
        d = g.home[c];
      } else if (pagina === '/nodos_idiomas/servicios') {
        d = g.servicios[c];
      } else if (pagina === '/nodos_idiomas/exitos') {
        d = g.exitos[c];
      }
      e = [];
      for (f in d) {
        h = d[f];
        if ($(f).get(0).tagName.toLowerCase() === "title") {
          document.title = h;
          continue;
        }
        if (f.length > 0) {
          e.push($(f).html(d[f]));
        } else {
          e.push(void 0);
        }
      }
      return e;
    });
  };
}.call(this));
